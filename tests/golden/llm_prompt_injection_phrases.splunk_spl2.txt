FROM main WHERE llm_prompt LIKE "%ignore previous instructions%" OR llm_prompt LIKE "%ignore all previous%" OR llm_prompt LIKE "%disregard the above%" OR llm_prompt LIKE "%disregard your instructions%" OR llm_prompt LIKE "%developer mode%" OR llm_prompt LIKE "%do anything now%" OR llm_prompt LIKE "%you are now DAN%" OR llm_prompt LIKE "%bypass your guidelines%" OR llm_prompt LIKE "%reveal your system prompt%"
